# A cost-sensitive uncertainty triage for SOC workload management for ransomware detection. — Python dependencies

# The original notebook was run in Google Colab without recording library versions. Therefore, the versions listed below represent the minimum versions required to support the APIs used in the notebook. For complete reproducibility, exact package versions should be pinned after performing a clean execution.

# Core data handling
pandas>=2.0
numpy>=1.24
pyarrow>=14.0

# Visualisation
matplotlib>=3.7
seaborn>=0.12

# Classical machine learning (RandomForest, GridSearchCV, RFE, mutual_info_classif, metrics, cost-sensitive boosting (scale_pos_weight), SMOTE)
scikit-learn>=1.3
xgboost>=2.0
imbalanced-learn>=0.11

# Deep learning
torch>=2.0
torchvision>=0.15

# Explainability, tuning, synthesis, statistics
shap>=0.44
optuna>=3.4
sdv>=1.9
statsmodels>=0.14

# Notebook environment
jupyter>=1.0
ipykernel>=6.25
