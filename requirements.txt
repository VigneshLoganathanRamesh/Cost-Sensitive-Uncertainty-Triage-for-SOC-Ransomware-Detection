# Cost-Sensitive Uncertainty Triage (CS-UT) — Python dependencies
#
# The original notebook was executed in Google Colab and did not print library
# versions, so the pins below are minimum versions known to expose every API the
# notebook uses. Pin exact versions after your own clean run for full reproducibility.

# Core data handling
pandas>=2.0
numpy>=1.24
pyarrow>=14.0            # required to read the .parquet datasets

# Visualisation
matplotlib>=3.7
seaborn>=0.12

# Classical machine learning
scikit-learn>=1.3        # RandomForest, GridSearchCV, RFE, mutual_info_classif, metrics
xgboost>=2.0             # cost-sensitive boosting (scale_pos_weight)
imbalanced-learn>=0.11   # SMOTE

# Deep learning
torch>=2.0               # 1D CNN, Focal Loss, MC Dropout
torchvision>=0.15

# Explainability, tuning, synthesis, statistics
shap>=0.44               # TreeExplainer + deep explanations
optuna>=3.4              # TPE hyperparameter search
sdv>=1.9                 # CTGANSynthesizer
statsmodels>=0.14        # McNemar's test

# Notebook environment
jupyter>=1.0
ipykernel>=6.25
